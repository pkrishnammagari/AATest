"""Derivations over the payload: joins and reductions shared by more than one
section.

Nothing here invents a value. Every function either returns something the bureau
delivered, something arithmetically derived from delivered values, or None --
and None means "not reported", which the renderer must show as such rather than
as a zero.
"""
